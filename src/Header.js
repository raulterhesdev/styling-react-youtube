import React from 'react';
import classes from './Header.module.css';

const Header = () => {
  return (
    <div>
      <h2 className={classes.header}>I am the Header</h2>
    </div>
  );
};

export default Header;
