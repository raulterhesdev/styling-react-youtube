import React from 'react';
import classes from './Footer.module.css';

const Footer = () => {
  return (
    <div>
      <h2 className={classes.header}>I am the footer</h2>
    </div>
  );
};

export default Footer;
